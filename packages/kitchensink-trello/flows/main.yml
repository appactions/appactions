description: 'Test recorded at 1/7/2022, 7:35:19 PM'
start:
  route: '/'
  auth: false
steps:
  - with: Board
  - with: { Lane: Planned Tasks, Button: /^Click to add card$/ }
    do: click
  - with: { NewCardForm, EditableLabel: title }
    do: { type: 'foo' }
  - with: { NewCardForm, EditableLabel: label }
    do: { type: 'bar' }
  - with: { NewCardForm, EditableLabel: description }
    do: { type: 'baz' }
  - with: { NewCardForm, Button: Add card }
    do: click

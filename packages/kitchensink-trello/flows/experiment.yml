description: 'Test recorded at 5/1/2022, 6:35:47 PM'
start:
  route: /
  auth: false
steps:
  - with:
      - Board
      - { Lane: Planned Tasks }
    do:
      - addCard: [foo, bar, baz]
  - with: Board
    do:
      - addLane: [aaa]
  - with:
      - Board
      - { Lane: aaa }
    do:
      - addCard: [fff]
      - assert: exists
  - with:
      - Board
      - { Lane: aaa }
    do:
      # - text: []
      # - { text: [], assert: fff }
      # - { text: [], expect: fff }
      # - { text: [] }: fff
      # - { text: fff }: []
      # - { text: fff }
      # - { text: { args: [], assert: fff } }
      # - assert:
      #     action: { text: [] }
      #     value: fff
      - assert:
          action:
            text: []
          value: fff
